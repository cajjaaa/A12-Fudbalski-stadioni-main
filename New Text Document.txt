            try
            {
                Konekcija();
                komanda.CommandText = "SELECT * FROM DRZAVE";
                da.SelectCommand = komanda;
                da.Fill(dt);

            for (int i = 0; i < dt.Rows.Count; i++){
                    drzave.Add(dt.Rows[i][1].ToString().ToLower());
            }
            string poruka = "";
            dt.Clear();
                foreach(string d in drzave)
                {
                    if (textBox1.Text.ToLower() == d)
                    {
                       // drzava = d;
                       poruka = "Postoji uneta drzava";
                    }
                    else if(textBox1.Text.ToLower()!=d)
                       poruka = "Ne postoji uneta drzava";
                    
                    else poruka = "Greska";
                }
                MessageBox.Show(poruka);

                /*konekcija.Close();

                if(Canceled == false)
                        Close();*/
            }
            finally{konekcija.Close();}